<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mms.usercenter.service.auth.mapper.RoleMapper">

    <!-- 根据用户ID查询角色编码集合（启用且未删除） -->
    <select id="selectRoleCodesByUserId" parameterType="long" resultType="java.lang.String">
        SELECT DISTINCT r.role_code
        FROM user_role ur
                 JOIN role r ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND (r.status = 1)
    </select>

</mapper>

