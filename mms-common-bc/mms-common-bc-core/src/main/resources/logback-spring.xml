<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 可在 application.yml/Nacos 中配置 logback.path 覆盖默认值；默认使用 logs 目录 -->
    <springProperty scope="context" name="LOG_PATH" source="logback.path" defaultValue="logs"/>

    <!--  设置控制台颜色  -->
    <conversionRule conversionWord="clr"
                    converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <!--  定义控制台和文件记录的日志格式，用于开发使用  -->
    <property name="CONSOLE_LOG_PATTERN"
              value="${CONSOLE_LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %X{traceId:-} %X{requestUri:-} [%thread] %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %L %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!--  定义日志文件的日志格式，用于生产使用  -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 追加日志到原文件结尾 -->
        <prudent>true</prudent>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度 %method 方法名  %L 行数 %msg：日志消息，%n是换行符-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %X{traceId:-} %X{requestUri:-} [%thread] %-5level %logger{56}.%method:%L - %msg%n</pattern>
            <charset>utf-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--日志文件名称定义，其中 %i 表示当前日志文件超过 maxFileSize 设置的值时进行文件切割的序号，从0开始，如 0、1、2、3、4...-->
            <FileNamePattern>${LOG_PATH}/%d{yyyy-MM-dd}.%i.log</FileNamePattern>
            <!--日志文件保留天数，超过该时间的日志将被自动清除-->
            <MaxHistory>30</MaxHistory>
            <!-- 每个日志文件的最大值，超过该值后，日志文件自动被切割，并且新的文件按照 FileNamePattern 设置的格式命名 -->
            <maxFileSize>100MB</maxFileSize>
            <!-- 所有的日志文件加起来总和不能超过该值，如果超过了，那么会先删除时间最旧的日志文件 -->
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 异步日志打印，可以提高系统TPS，默认是同步打印-->
    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <!--    解决异步打印时class行号不打印问题    -->
        <includeCallerData>true</includeCallerData>
        <!--    日志队列大小，默认 256    -->
        <queueSize>1000</queueSize>
    </appender>

    <!-- 日志级别交由外部 logging.level 管控（Nacos 或本地配置） -->
    <!-- 根据环境变量决定是否使用异步日志：开发环境使用同步，生产环境使用异步 -->
    <springProfile name="!DEV">
        <!-- 生产/测试环境：使用异步日志提高性能 -->
        <root level="INFO">
            <appender-ref ref="ASYNC"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
    
    <springProfile name="DEV">
        <!-- 开发环境：使用同步日志，便于调试，控制台实时输出 -->
        <root level="INFO">
            <appender-ref ref="FILE"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>


